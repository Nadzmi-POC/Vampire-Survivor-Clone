using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public struct OnStageTimerStart {
    public float timerDuration;

    public OnStageTimerStart(float timerDuration) {
        this.timerDuration = timerDuration;
    }
}
